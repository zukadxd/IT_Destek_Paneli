@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Kayıt Ol - IT Destek Paneli</title>
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
            body {
                background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
                height: 100vh;
                display: flex;
                align-items: center;
                justify-content: center;
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                margin: 0;
            }

            .register-card {
                background: rgba(255, 255, 255, 0.95);
                border-radius: 20px;
                box-shadow: 0 15px 35px rgba(0,0,0,0.5);
                width: 100%;
                max-width: 400px;
                padding: 2.5rem;
            }

            .form-control {
                border-radius: 10px;
                padding: 0.8rem;
                border: 1px solid #ddd;
            }

                .form-control:focus {
                    border-color: #2c5364;
                    box-shadow: 0 0 0 0.25rem rgba(44, 83, 100, 0.25);
                }

            .btn-register {
                background-color: #2c5364;
                border: none;
                border-radius: 10px;
                padding: 0.8rem;
                font-weight: bold;
                color: white;
                transition: all 0.3s;
            }

                .btn-register:hover {
                    background-color: #203a43;
                    transform: translateY(-2px);
                }
    </style>
</head>
<body>

    <div class="register-card text-center">
        <div class="mb-4">
            <i class="fa-solid fa-user-plus fa-3x" style="color: #2c5364;"></i>
            <h3 class="fw-bold mt-3" style="color: #0f2027;">Yeni Hesap Oluştur</h3>
            <p class="text-muted small">Sisteme katılmak için bilgilerinizi giriniz</p>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger py-2 small shadow-sm">
                <i class="fa-solid fa-triangle-exclamation me-2"></i> @ViewBag.Error
            </div>
        }

        <form action="/Account/Register" method="post">
            <div class="text-start mb-3">
                <label class="form-label small fw-bold text-secondary">Kullanıcı Adı Belirleyin</label>
                <div class="input-group">
                    <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-user text-muted"></i></span>
                    <input type="text" name="username" class="form-control border-start-0 bg-light" placeholder="Kullanıcı adınızı giriniz" required autocomplete="off">
                </div>
            </div>

            <div class="text-start mb-4">
                <label class="form-label small fw-bold text-secondary">Şifre Belirleyin</label>
                <div class="input-group">
                    <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-lock text-muted"></i></span>
                    <input type="password" name="password" class="form-control border-start-0 bg-light" placeholder="6-20 karakter arası giriniz" required minlength="6" maxlength="20">
                </div>
            </div>

            <button type="submit" class="btn btn-register w-100 shadow-sm">
                Kayıt Ol <i class="fa-solid fa-check ms-1"></i>
            </button>
        </form>

        <div class="mt-4 pt-3 border-top text-muted small">
            Zaten hesabınız var mı? <a href="/Account/Login" class="text-decoration-none fw-bold" style="color: #203a43;">Giriş Yap</a>
        </div>
    </div>

</body>
</html>